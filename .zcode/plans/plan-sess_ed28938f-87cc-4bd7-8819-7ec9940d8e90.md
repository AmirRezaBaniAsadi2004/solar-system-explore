Continue building the Solar System Roadmap Explorer. Foundation is complete (data layer, CelestialBody, InfoCard, StarField, RoadmapSection with GSAP animations, gsap+lenis installed). Remaining work:

**1. Hooks**
- `src/hooks/useLenis.js` — creates Lenis, ties it into the GSAP ticker + ScrollTrigger.update, returns a stable ref so nav components can call `lenisRef.current.scrollTo(target)`; exposes stop/start for overlay lock.
- `src/hooks/useBodyProgress.js` — on scroll, computes the active body (section nearest viewport center) + overall progress ratio (0–1) for the header bar.

**2. Navigation components**
- `src/components/ProgressIndicator.jsx/.css` — fixed glassy header: "SUN → PLUTO" progress bar fill, current body name + distance in AU, "leg x/10" counter. Collapses gracefully on mobile.
- `src/components/NavDots.jsx/.css` — right-edge vertical dots (one per body, body-accent colored on hover with name tooltip); active dot glows; click scrolls via Lenis. Hidden on small screens.

**3. DetailView**
- `src/components/DetailView.jsx/.css` — full-screen fixed overlay with its own scroll container: hero with large CelestialBody + name/type/tagline; stat grid (Type, Distance, Size, Temperature, Rotation, Orbital period); prose sections (Overview, Physical characteristics, Atmosphere); facts list; missions timeline; moons cards (skipped when empty); footer with Previous/Next body buttons + "Back to roadmap". GSAP entrance (overlay fade + content stagger); Esc closes, ←/→ switch bodies; switching animates content re-entrance. Uses existing flat data schema.

**4. App shell**
- `src/App.jsx` — composes StarField, ProgressIndicator, NavDots, hero intro ("Scroll to begin at the Sun"), one RoadmapSection per body from `celestialBodies`, end card after Pluto ("You've reached the edge" + return-to-Sun button); holds `activeBodyId` + `openDetailId` state; overlay open → `lenis.stop()` + body scroll lock, close → `lenis.start()`.
- `src/App.css` — hero/end layout styles.
- `index.html` — proper title/meta.

**5. Verify**
- `npm run dev`, browser-check scroll animations, "More..." overlay, prev/next, responsive breakpoints; `npm run build` for a clean production build.

No changes to already-built components; everything follows the established data schema and styling conventions.